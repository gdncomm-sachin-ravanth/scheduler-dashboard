# scheduler-dashboard
Scheduler Dashboard gives you a quick look at all schedulers and their latest runs. See how many jobs ran, which ones failed, and when they are scheduled. Heatmaps, tables, and quick stats make it easy to spot issues fast and drill into details like curl commands when you need them.
